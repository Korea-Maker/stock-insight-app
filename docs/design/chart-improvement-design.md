# 차트 개선 설계 문서

## 1. 요구사항 분석

### 1.1 현재 문제점
1. **마우스 호버 시 동그란 표시**: 보조 지표에 마커가 표시됨 (crosshair 관련)
2. **제한된 기능**: 사용자가 지표를 빠르게 확인/수정할 수 없음
3. **좌측 상단 정보 부족**: 현재 활성화된 지표 정보가 차트에 직접 표시되지 않음

### 1.2 개선 요구사항
1. 보조 지표의 `lastValueVisible` 마커 제거
2. 차트 좌측 상단에 활성 지표 정보 표시 (Active Indicator Legend)
3. 좌측 상단에서 지표 설정 빠른 수정 기능
4. 우측 상단 Indicators UI 유지

---

## 2. 설계 개요

### 2.1 컴포넌트 구조

```
TradingChart.tsx
├── ChartControls.tsx (우측 상단 - 기존 유지)
├── ActiveIndicatorLegend.tsx (NEW - 좌측 상단)
│   ├── IndicatorBadge (각 지표별 뱃지)
│   ├── QuickEditPopover (빠른 수정 팝오버)
│   └── IndicatorValueDisplay (실시간 값 표시)
├── IndicatorSettingsPanel.tsx (우측 패널 - 기존 유지)
└── Chart containers...
```

### 2.2 데이터 흐름

```
┌────────────────────────────────────────────────────────────────┐
│                        TradingChart                            │
│  ┌──────────────────────┐     ┌────────────────────────────┐  │
│  │ ActiveIndicatorLegend│     │      ChartControls         │  │
│  │ (좌측 상단)           │     │      (우측 상단)           │  │
│  │ - 활성 지표 목록      │     │ - Symbol/Interval         │  │
│  │ - 실시간 값           │     │ - Drawing tools           │  │
│  │ - 빠른 수정 팝오버    │     │ - Indicators 버튼          │  │
│  └──────────────────────┘     └────────────────────────────┘  │
│                                                                │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │                    Main Chart                            │  │
│  │  - lastValueVisible: false (마커 제거)                   │  │
│  │  - crosshairMarkerVisible: false                         │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │                    Sub Panels (RSI, MACD, etc.)          │  │
│  └─────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────┘
```

---

## 3. 상세 설계

### 3.1 마커 제거 설정 (TradingChart.tsx 수정)

```typescript
// 모든 LineSeries 생성 시 마커 비활성화
const series = chart.addSeries(LineSeries, {
  color: config.color,
  lineWidth: config.lineWidth,
  priceLineVisible: false,
  lastValueVisible: false,          // 우측 가격 축 마커 제거
  crosshairMarkerVisible: false,    // 크로스헤어 마커(동그란 점) 제거
});
```

**적용 대상:**
- MA Series (maSeriesRefs)
- Ichimoku Series (ichimokuRefs)
- Bollinger Bands (overlaySeriesRef.bb*)
- VWAP (overlaySeriesRef.vwap*)
- Supertrend (overlaySeriesRef.supertrendLine)
- EMA Ribbon (overlaySeriesRef.emaRibbonSeries)
- RSI Series (subChartsRef.rsiSeries)
- MACD Series (macdLine, macdSignal)
- Stochastic Series (stochasticK, stochasticD)
- ATR Series (atrSeries)
- ADX Series (adxSeries, plusDI, minusDI)
- OBV Series (obvSeries)

### 3.2 ActiveIndicatorLegend 컴포넌트

**파일 위치:** `frontend/components/Chart/ActiveIndicatorLegend.tsx`

```typescript
interface ActiveIndicatorLegendProps {
  // 활성화된 지표 데이터
  movingAverages: MovingAverageConfig[];
  ichimoku: IchimokuConfig;
  bollingerBands: BollingerBandsConfig;
  vwap: VWAPConfig;
  supertrend: SupertrendConfig;
  parabolicSAR: ParabolicSARConfig;
  emaRibbon: EMARibbonConfig;

  // RSI 및 서브패널 지표
  rsiConfigs: RSIConfig[];
  macd: MACDConfig;
  stochastic: StochasticConfig;
  atr: ATRConfig;
  adx: ADXConfig;
  obv: OBVConfig;

  // 실시간 값 (optional)
  currentValues?: IndicatorCurrentValues;

  // 수정 콜백
  onUpdateMA: (id: string, updates: Partial<MovingAverageConfig>) => void;
  onToggleMA: (id: string) => void;
  onRemoveMA: (id: string) => void;
  // ... 기타 지표별 콜백
}
```

**UI 구조:**

```
┌─ Active Indicators ────────────────────────────────────┐
│ ┌─────────────────────────────────────────────────────┐│
│ │ 📈 Overlays                                         ││
│ │ ┌────────┐ ┌────────┐ ┌────────┐                   ││
│ │ │SMA 20  │ │EMA 50  │ │BB(20,2)│ ...               ││
│ │ │ 95,432 │ │ 94,821 │ │[hover] │                   ││
│ │ └────────┘ └────────┘ └────────┘                   ││
│ └─────────────────────────────────────────────────────┘│
│ ┌─────────────────────────────────────────────────────┐│
│ │ 📊 Oscillators                                      ││
│ │ ┌────────┐ ┌─────────────────┐                     ││
│ │ │RSI(14) │ │MACD(12,26,9)    │ ...                 ││
│ │ │  58.3  │ │ +245 / Signal   │                     ││
│ │ └────────┘ └─────────────────┘                     ││
│ └─────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────┘
```

### 3.3 IndicatorBadge 컴포넌트

```typescript
interface IndicatorBadgeProps {
  type: 'ma' | 'rsi' | 'macd' | 'bb' | 'vwap' | 'supertrend' | 'ichimoku' | ...;
  label: string;           // "SMA 20", "RSI(14)", etc.
  color: string;           // 지표 색상
  value?: number | string; // 현재 값 (optional)
  enabled: boolean;

  // Quick edit
  onToggle: () => void;
  onRemove?: () => void;
  onEdit?: () => void;     // 팝오버 열기
}
```

**뱃지 디자인:**

```
┌──────────────────┐
│ ● SMA 20  95,432 │  <- 색상 점 + 레이블 + 값
│     [×]   [⚙]   │  <- 호버 시 제거/설정 버튼
└──────────────────┘
```

### 3.4 QuickEditPopover 컴포넌트

**빠른 수정 팝오버 (클릭 시 표시):**

```typescript
interface QuickEditPopoverProps {
  indicator: IndicatorConfig;
  onUpdate: (updates: Partial<IndicatorConfig>) => void;
  onClose: () => void;
}
```

**MA 예시:**
```
┌─ SMA 20 ────────────────────┐
│ Type:   [SMA ▼] [EMA ▼]    │
│ Period: [____20____] [-][+]│
│ Color:  [●●●●●●●●]         │
│ Width:  [1] [2] [3]        │
│ ──────────────────────────│
│ [Remove]          [Apply] │
└─────────────────────────────┘
```

### 3.5 Store 확장 (useChartStore.ts)

현재 값 저장을 위한 상태 추가:

```typescript
interface IndicatorCurrentValues {
  // MA 값들
  maValues: Record<string, number>;  // { [maId]: value }

  // 서브패널 값들
  rsiValues: Record<string, number>;
  macdValue: { macd: number; signal: number; histogram: number } | null;
  stochasticValue: { k: number; d: number } | null;
  atrValue: number | null;
  adxValue: { adx: number; plusDI: number; minusDI: number } | null;
  obvValue: number | null;

  // Overlay 값들
  bbValue: { upper: number; middle: number; lower: number } | null;
  vwapValue: number | null;
  supertrendValue: number | null;
}

interface ChartState {
  // ... 기존 상태
  currentIndicatorValues: IndicatorCurrentValues;
}

interface ChartActions {
  // ... 기존 액션
  updateCurrentIndicatorValues: (values: Partial<IndicatorCurrentValues>) => void;
}
```

---

## 4. 구현 계획

### Phase 1: 마커 제거
1. TradingChart.tsx의 모든 LineSeries에 `crosshairMarkerVisible: false` 추가
2. `lastValueVisible: false` 확인 및 적용

### Phase 2: ActiveIndicatorLegend 기본 구현
1. 컴포넌트 파일 생성
2. 활성 지표 목록 표시
3. 기본 스타일링

### Phase 3: 실시간 값 표시
1. crosshair 이벤트 구독하여 현재 값 추출
2. Store에 currentIndicatorValues 추가
3. 뱃지에 값 표시

### Phase 4: 빠른 수정 기능
1. QuickEditPopover 구현
2. 각 지표별 빠른 설정 UI
3. 토글/제거 기능

---

## 5. 파일 변경 목록

### 새로 생성:
- `frontend/components/Chart/ActiveIndicatorLegend.tsx`
- `frontend/components/Chart/IndicatorBadge.tsx`
- `frontend/components/Chart/QuickEditPopover.tsx`

### 수정:
- `frontend/components/Chart/TradingChart.tsx`
  - Series 옵션에 `crosshairMarkerVisible: false` 추가
  - ActiveIndicatorLegend 컴포넌트 통합
  - crosshair 이벤트 구독 추가

- `frontend/store/useChartStore.ts`
  - currentIndicatorValues 상태 추가
  - updateCurrentIndicatorValues 액션 추가

---

## 6. UI 목업

### 전체 레이아웃:

```
┌─────────────────────────────────────────────────────────────────────────┐
│ ┌─ Active Indicators ───┐                    ┌── Controls ────────────┐│
│ │ SMA 20   EMA 50       │                    │ BTC ETH SOL BNB XRP   ││
│ │ BB(20,2) VWAP         │                    │ 1m 5m 15m 1h 4h 1d    ││
│ │ RSI(14): 58.3         │                    │ [─] [↗] [⚙ Indicators]││
│ └───────────────────────┘                    └────────────────────────┘│
│                                                                         │
│ ┌─────────────────────────────────────────────────────────────────────┐│
│ │                                                                     ││
│ │                         Main Chart                                  ││
│ │                                                                     ││
│ └─────────────────────────────────────────────────────────────────────┘│
│ ┌─────────────────────────────────────────────────────────────────────┐│
│ │ RSI (14)                                                            ││
│ └─────────────────────────────────────────────────────────────────────┘│
│ ┌─────────────────────────────────────────────────────────────────────┐│
│ │ MACD (12, 26, 9)                                                    ││
│ └─────────────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────────────┘
```

### 지표 뱃지 상세:

```
일반 상태:
┌─────────────┐
│ ● SMA 20    │  <- 색상 + 이름
└─────────────┘

호버 상태:
┌─────────────────┐
│ ● SMA 20  ⚙ ×  │  <- 설정/제거 아이콘 표시
└─────────────────┘

값 표시 (크로스헤어 hover 시):
┌─────────────────────┐
│ ● SMA 20    95,432  │  <- 현재 값 표시
└─────────────────────┘
```

---

## 7. 기술 고려사항

### 7.1 성능 최적화
- 크로스헤어 이벤트는 throttle 적용 (16ms / 60fps)
- 값 계산은 메모이제이션 사용
- 뱃지 컴포넌트는 React.memo로 최적화

### 7.2 반응형 디자인
- 지표가 많을 경우 스크롤 또는 접기 기능
- 모바일에서는 간소화된 뷰

### 7.3 접근성
- 키보드 네비게이션 지원
- 색상 대비 확보
- ARIA 라벨 추가

---

## 8. 테스트 계획

1. **마커 제거 테스트**: 모든 지표에서 동그란 마커가 사라졌는지 확인
2. **레전드 표시 테스트**: 활성화된 지표만 표시되는지 확인
3. **빠른 수정 테스트**: 팝오버에서 설정 변경이 차트에 반영되는지 확인
4. **실시간 값 테스트**: 크로스헤어 이동 시 값이 업데이트되는지 확인
5. **반응형 테스트**: 다양한 화면 크기에서 레이아웃 확인
